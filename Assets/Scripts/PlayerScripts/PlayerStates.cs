using UnityEngine;
using UnityEngine.InputSystem;

//public interface IPlayerStates
//{
//    void OnEnter(PlayerController player); //call once when enter to this state

//    void OnExit(PlayerController player); //call once when exit from this state

//    void HandleMovement(PlayerController player, InputAction.CallbackContext context); // Handles continuous input (eg. movement)

//    void HandleJump(PlayerController player, InputAction.CallbackContext context); // Handles disposable input (eg. jumping)

//    //void WallCling(PlayerController player, InputAction.CallbackContext context); // Only when Raycast in PlayerController touch Walls

//    void PhysicsUpdate(PlayerController player); // uses only for physics-based updates in FixedUpdate
//}
